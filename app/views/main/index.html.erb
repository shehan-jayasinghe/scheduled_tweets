<div class="d-flex d-flex justify-content-center">
    <h1>Whlcome to tweeter</h1>
</div>


<% if @user %>
   Logged in as: <%= @user.email%>
   <%= button_to "Logout", logout_path, method: :delete %>
<% end %> 